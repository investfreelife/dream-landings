# Jekyll отключён — мы пушим уже готовый статический HTML.
# Этот файл нужен только чтобы GitHub Pages НЕ запускал Jekyll
# (иначе папки начинающиеся с _ скрываются).
plugins: []
exclude: []
include: ['.htaccess']
